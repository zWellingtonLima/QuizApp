const initialButton = document.querySelector('[data-initialButton="btn"]');
const introPage = document.querySelector('.intro');


initialButton.addEventListener('click', () => {
  // introPage.classList.add('d-none')
})